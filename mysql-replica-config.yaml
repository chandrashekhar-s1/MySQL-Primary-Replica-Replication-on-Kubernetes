apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-replica-config
  namespace: mysql
data:
  mysql.cnf: |
    [mysqld]
    server-id=2
    log-bin=mysql-bin
    binlog-format=ROW
    gtid-mode=ON
    enforce-gtid-consistency=ON
    read_only=ON
    bind-address=0.0.0.0
